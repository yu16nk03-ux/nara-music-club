# NMC v3.0 セッション別課題曲

1. まず GitHub Pages にこのZIPの中身を上書きアップロード。
2. Supabase の SQL Editor で `NMC-v3.0-Supabase-session.sql` を全文貼り付けて Run。
3. 管理画面 → 「セッション管理」で、例えば「9月セッション」「10月セッション」を作成。
4. 日付が決まっていれば開催日を入力。未定でも作成可能。
5. そのセッションで使う課題曲だけチェックして作成。
6. メンバー画面ではセッションを切り替えると、そのセッションの課題曲だけが表示され、希望パートを登録できます。
7. 開催日を設定した場合、前日23:59:59を過ぎると希望パートは自動的に変更不可（閲覧のみ）になります。

既存の8月29日セッションと現在の希望パートは、SQL実行時に自動で引き継ぎます。
