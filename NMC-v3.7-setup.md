# NMC v3.7 テスト運営後の改善

## 今回の変更
1. 公開ホームページの「ライブ・出演情報」
   - 開催日が今日より前のイベントは表示しません。
   - 開催日未定のイベントは「COMING SOON」として表示します。
   - 公開一覧は最大5件です。

2. 運営管理画面
   - メンバーが「運営にする」だけでなく「運営から外す」こともできます。
   - 会員承認はそのままで、運営権限だけ外れます。
   - 運営が0人になるのを防ぐため、最後の1人は外せません。

3. 活動記録
   - これまで通りセッション管理に登録されたセッションから活動記録を作成できます。
   - 新しく「＋ 過去の活動を追加」から、セッション管理にない過去の活動も追加できます。
   - 過去活動では、活動名・開催日・演奏した曲・活動メモ・YouTube URL・写真を登録できます。

## Supabase
付属の `NMC-v3.7-Supabase-test-operation-fixes.sql` をSupabase SQL Editorで1回実行してください。

このSQLでは、既存の活動記録を壊さないように、既存セッションの記録には活動日・活動名・演奏曲を自動補完します。

## GitHub Pages
ZIPの中身を、現在使っているGitHub Pagesリポジトリへ上書きアップロードしてください。

既存の `images` フォルダや写真はそのまま残してください。
