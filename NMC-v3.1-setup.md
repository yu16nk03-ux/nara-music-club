# NMC v3.1 セットアップ

v3.1では管理者向け「🎸 当日運営」を追加しました。

- セッションごとの曲・希望パートを一覧表示
- 希望パートを出しているメンバーを会計対象として自動登録
- 会計チェックは管理者だけ操作可能
- 支払済み人数／対象人数を自動表示

## Supabase
`NMC-v3.1-Supabase-day.sql` をSupabaseのSQL Editorで1回実行してください。

v3.0までの設定はそのまま利用します。
